tech stack: PHP backend, Bootstr 5 frontend, MySQL database, docker architecture
MUST HAVES:
-table for 'users' and a table for 'teas'
- auth, login register page
- validation where i use php to check if the price field is a number before saving to DB

PERSONAL TOUCH:
- user roles, column in database 'role'. ADMIN: see the delete buttons, 
                                       USER: only see buy BTNS
- search bar and filter, for tea categories
- docker - wrapping the project in a container
EXTRA SHIIZ:
- custom tea builder, form where users pick a base and toppings. uses modal popup window
                                             so the page doesn't reload
- AJAX FETCH: when user clicks add to card, using a small snippet of JavaScript
                                            maybe fetch api to show a 'Success' 
                                            msg without the whole page reloading/blinking
- Dark/Green mode toggle, that switches from a light green mode to a really dark green mode
kinda like the dark mode but with green opposites